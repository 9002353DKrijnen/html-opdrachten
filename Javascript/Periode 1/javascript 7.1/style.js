function calculate ( par1,par2 ) {
    	let resulte = (par1 + par2);
        console.log(resulte);
}
function multiplyByFive (par ) {
    let resulte = (par * 5);
    console.log(resulte);
}
function calculateMinutes (par) {
    let resulte = (par / 60);
    console.log(resulte);
}
